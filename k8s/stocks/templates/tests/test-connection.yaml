apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stocks.fullname" . }}-test-connection"
  labels:
{{ include "stocks.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "stocks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
